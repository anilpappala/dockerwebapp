version: "3.9"

services:
  devopsdb:
    image: anilpappala/project:db
    ports:
      - "3306:3306"

  application:
    image: anilpappala/project:app
    ports:
      - "1111:8080"
    depends_on:
      - devopsdb
